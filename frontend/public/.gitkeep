Keeps the web/ directory present so `go:embed all:web` always has a target,
even before the frontend has been built. Vite copies publicDir into web/, so
this file survives `emptyOutDir` on every build.
